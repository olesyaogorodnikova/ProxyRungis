<%= simple_form_for([ package, cart_item ]) do |form| %>
  <%= form.input :quantity, as: :integer, input_html: { min: '0', step: 'any' }, prompt: "Quantité" %>
  <%= form.button :submit, "Ajouter", class: "btn btn-primary"%>
<% end %>
