<body>
  <div id="container1" class="container" style="height: 300px">
    <h1 class = "mb-5 text-center"> Quand souhaitez vous récupérer vôtre commande ? </h1>
  </div>
  <div class="container">
  <div class="form-wrapper" style="width: 400px; margin: 0 auto;">
    <h2>Entre la date à laquelle tu veux récupérer ton panier:</h2>

    <%= simple_form_for @order, method: "get" do |f| %>
      <%= f.input :starts_at, as: :string, required: false, input_html: {class: "datepicker"} %>
      <%= f.input :end_at, as: :string, required: false, input_html: {class: "datepicker"} %>
      <%= f.button :submit, "Go", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

  <!-- Code de Gauthier
  </div>
  <div id="address" class="form-group">
    <label for="inputAddress">Adresse</label>
    <input type="text" class="form-control" id="inputAddress" placeholder="11 rue oberkampf">
  </div>
        -->
  <div>
    <h2 class = "mb-5 text-center" style="color: #727372"> On livre à moins de 500 mètres de chez vous ! </h2>
    <h2 class = "mb-5 text-center" style="color: #727372"> Voir les points de retraits à proximité de chez moi :</h2>
  </div>
  <div
    id="map"
    style="width: 80%;
    height: 600px;"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
  <div>
    <div id="button2"> <%= link_to "Let's Go !", orders_path, class: "btn btn-primary btn-lg btn-flat p-3  pl-5 pr-5 font-weight-bold" %>
    </div>
  </div>
</body>
