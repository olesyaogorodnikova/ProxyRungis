<h1 class = "mb-5 text-center"> Quand souhaitez vous récupérer vôtre commande ? </h1>
<div class="container">
  <div class="form-wrapper" style="width: 400px; margin: 0 auto;">
      <%= simple_form_for @order do |f| %>
        <%= f.input :day, as: :string, required: true, input_html: {class: "datepicker"} %>
        <%= f.input :time_start, as: :time, required: true, input_html: {class: "timepicker"} %>
        <%= f.input :time_end, as: :time, required: true, input_html: {class: "timepicker"} %>
        <%= f.input :address, as: :string, required: true, default: "16 villa gaudelet" %>
      </div>
  </div>
</div>
<div>
  <h2 class = "mb-5 text-center" style="color: #727372"> Voir les points de retraits à proximité :</h2>
</div>
<div
  id="map"
  data-markers="<%= @markers.to_json %>";
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
  data-center="<%= @center  %>">
  </div>
<div class=" container form-wrapper" style="text-align: center; padding: 20px">
  <%= f.button :submit, "Let's Go", class: "btn btn-primary btn-lg btn-flat p-3  pl-5 pr-5 font-weight-bold" %>
  <% end %>
</div>
